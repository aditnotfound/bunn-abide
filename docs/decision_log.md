# Decision log

Record every protocol decision before inspecting its corresponding final test result.

| Date | Decision | Rationale | Evidence / commit |
| --- | --- | --- | --- |
| 2026-08-03 | Use ABIDE-I PCP `cpac/filt_noglobal/rois_aal` as the first manifest derivative path. | Matches the narrowed operator-comparison protocol while avoiding global-signal-regression sign dependence in the primary run. | `scripts/prepare_abide_manifest.py`; `docs/step5_abide_manifest.md`. |
| 2026-08-03 | Freeze the primary ABIDE-I manifest as `rater1_and_func2_ok`, with available `FILE_ID`, all derivative URLs checked, and at least one ASD and one control per site. | Combines the PCP overall and functional QC flags, retains the available multi-site cohort, and prevents undefined held-out-site balanced-accuracy folds. `LEUVEN_2` (0 ASD, 6 controls) and `SBL` (1 ASD, 0 controls) are excluded before any model results. | `configs/abide_i_primary_manifest.json`; manifest SHA-256 `ce3bdbfa02589629a687c341cb5c69fe176a7be5d2ad913d7d510665c093f39b`. |
| 2026-08-03 | Derive a separate 754-subject analysis manifest by excluding only time-series files that cannot yield a finite 116-region Pearson/Fisher-z connectome. | Fifteen of 769 frozen-manifest files had at least one zero-variance ROI. Retaining them would make correlations undefined; removing arbitrary regions or filling values would alter the AAL representation. All 18 sites retain both classes after the technical filter. | `configs/abide_i_analysis_manifest.json`; analysis manifest SHA-256 `bef94eb5d144a445f253f5828bedf452b1c8343c745417766d695fb206f05945`. |
| 2026-08-03 | Freeze the Step 7 baseline protocol before fitting any classifier: 18 outer leave-one-site-out folds, four grouped inner folds, fixed threshold, and train-only preprocessing/tuning. | The primary claim requires a fair unseen-site reference. Site ID, FIQ, harmonization, PCA, and external feature selection are excluded from the baseline phase. | `configs/baseline_protocol.json`. |
| 2026-08-03 | Freeze generated baseline inputs and split assignments after successful independent audit and deterministic regeneration. | The 754-row baseline table aligns exactly with the connectome artifact. The 18 outer and 12,818 inner assignments contain no subject or site leakage and have valid two-class test/validation partitions. | `configs/baseline_inputs_and_splits.json`; outer SHA-256 `cb3ecae86cd12d48802fcadece87a51e4e128144d35c180cd66a2abf468f6cef`; inner SHA-256 `7fc2311792689d854850a81f8c6a78e5b8f646d7dba9d17788114be000a7c7f7`. |
| 2026-08-03 | Permit a separately labelled `--fast-smoke` execution mode for Step 7.4 only. | A two-site engineering check must verify all pipelines and grouped-split mechanics without silently running an incomplete expensive scientific analysis. The override preserves the real data and folds but limits candidates and elastic-net iterations; it is blocked for full runs and its outputs cannot support conclusions. | `scripts/run_baselines.py`; `docs/step7_baselines.md`; run `step7_4_fast_smoke_nyu_caltech`. |
| 2026-08-03 | Require atomic site checkpoints and immutable-contract resume validation before the full baseline run. | A long full run must be recoverable without mixing code, data, split, or protocol versions. A completed site is sealed only after its artifacts and hashes are present; interrupted sites are rerun rather than partially reused. | `scripts/run_baselines.py`; `scripts/check_baseline_run.py`; `scripts/launch_managed_baseline.sh`; `docs/step7_run_control.md`. |
| 2026-08-05 | Retire the pre-fit `step7_5_full_baselines_v1` launch and start a new `v2` run after correcting SNS region selection. | `v1` stopped at the required STARTED notification with `NoRegionError`, before any fit, prediction, metric, or completed site. The correction derives the SNS region from the private topic ARN and is regression-tested. A new run ID prevents combining artifacts across execution code. | Commit `7ad7246`; `docs/step7_5_full_baseline_plan.md`; AWS `v1` status/log. |
| 2026-08-05 | Launch `step7_5_full_baselines_v2` as the primary full baseline execution. | All prelaunch checks passed: frozen artifacts, matching deployment hashes, 13 intended tests, private SNS configuration, instance role, storage, and no existing `v2` run. The required STARTED notification published before the first model fit. | AWS run status at `2026-08-05T08:01:24Z`; PID `40909`; code identifier `7ad7246`. |
| 2026-08-07 | Retain `step7_5_full_baselines_v2` as the audited baseline artifact and keep its performance results embargoed while Step 7.6 analysis is implemented on synthetic data. | The score-blind auditor passed sealed-fold hashes, aggregate hashes, frozen-input validation, schemas, participant/model coverage, numerical ranges, tuning selection, and expected row counts. Building the analysis module before reading results prevents outcome-driven summaries. | `scripts/audit_baseline_run.py`; local ignored archive SHA-256 `bc8fd504...e4476`; audit certificate SHA-256 `9d768758...fe16b`. |
| 2026-08-07 | Freeze the Step 7.6 baseline analysis implementation before reading the completed baseline outputs. | The implementation recomputes metrics from held-out participant predictions, verifies saved metric consistency, uses the protocol-fixed 10,000 paired site bootstrap, emits the pre-specified tables/figures, and requires an exact run-ID acknowledgement to unblind. | Analysis commit `7795e1b2fa5fedcc94e5a169daeef6c26e37a300`; `scripts/analyze_baselines.py` SHA-256 `558b654404657e1c923e2d7e4e57b5e5382e14d647e9cc3a48a4aec3791fd9a1`; input run archive SHA-256 `bc8fd504...e4476`; 17 AWS tests passed. |
| 2026-08-07 | Preserve all Step 7.6 estimands and output templates while correcting the selected-candidate CSV parser to accept the runner's documented `0`/`1` flags. | The first acknowledged invocation stopped at tuning-row validation before creating an output directory or emitting any performance value. Inspection of the existing runner source showed it writes `selected = int(...)`; the correction is a compatibility fix, rejects unknown flags, and is covered by the synthetic-data suite. | `scripts/run_baselines.py:588`; compatibility-fix commit `30b9b4c32d8357c3614dce0fc54f9793bea38058`; analysis SHA-256 `124117b84e08189d48a0063cd42ce765a83a7b51418ad9da97c1088f6e3bf45c`; 17 AWS tests passed after the correction. |
| 2026-08-07 | Treat the connectome-only elastic-net model as the empirically strongest non-graph reference for Phase 8, while retaining covariates-only and combined elastic-net controls in every neural comparison. | Its primary site-weighted balanced accuracy was higher than both alternatives; the paired interval for its advantage over covariates excluded zero, while the interval against combined elastic net crossed zero. This is a baseline result under the frozen ABIDE-I pipeline, not evidence of biological mechanism or clinical utility. | `docs/step7_6_baseline_results.md`; archived Step 7.6 analysis package SHA-256 `c60d4f...3267a`. |
| 2026-08-07 | Freeze the Phase 8 graph/operator semantics before any neural training: binary deterministic top-positive graphs; five fixed densities; exact dense heat diffusion; identity, GCN, trivial-bundle, and learned O(2)-BuNN operators; and common-frame diagnostics. | The 116-node graphs are small enough for exact dense heat kernels. The trivial-bundle control distinguishes diffusion from learned transport, while common-frame diagnostics avoid treating arbitrary local rotations as representation diversity. The learned-map capacity difference is reported rather than silently width-matched away. | `configs/neural_operator_contract.json`; `src/neural_data.py`; `src/neural_operators.py`; `docs/step8_neural_implementation.md`. |
| 2026-08-07 | Freeze a score-blind Phase 9 neural engineering pilot before any neural held-out-site evaluation. | The pilot fixes one outer split with CALTECH excluded from model tensors, inner fold 0 for validation, the four frozen operators, five frozen densities, 3 epochs, batch size 8, AdamW (`1e-3`, `1e-4`), and seed `20260803`. It can establish implementation stability, resource use, and resume behavior, but cannot select a model or support a predictive conclusion because it logs loss only and does not calculate held-out-site predictions or metrics. | `configs/neural_pilot_protocol.json`; `scripts/run_neural_pilot.py`; `docs/research_execution_plan.md`. |
| 2026-08-07 | Retire the partial `step9_neural_pilot_v1` engineering artifact and accept only `step9_neural_pilot_v2` for Phase 9 recovery evidence. | `v1` exposed a resume bug after an intentional checkpoint interruption. The correction changes executable code, so immutable-run rules forbid mixing its checkpoint with the corrected code. `v2` repeats the intentional interruption under a single code identity, resumes, completes all 20 cells, and passes the independent integrity audit. Neither artifact contains a held-out-site predictive result. | Commits `727c557`, `22b2ec2`; `docs/step9_neural_pilot.md`; AWS `step9_neural_pilot_v2/integrity_audit.json`. |
| 2026-08-07 | Add learned-local as the same-capacity no-diffusion control and freeze the full nested neural and analysis protocols before accepted held-out neural results. | Learned BuNN has 2,640 map-generator parameters beyond identity/GCN/trivial bundle. Learned-local retains those maps and updates but removes inter-node diffusion, separating transport benefit from added node-wise capacity. The 14-configuration matrix, four-candidate equal tuning budget, grouped inner selection, stopping/refit rule, five final seeds, curve-level primary estimand, gauge-aware endpoint, and results embargo are fixed in versioned JSON contracts. | `configs/neural_operator_contract_v2.json`; `configs/neural_full_protocol.json`; `configs/neural_analysis_protocol.json`; `docs/step10_neural_full_plan.md`. |
| 2026-08-07 | Accept `step10_neural_full_smoke_v2` as the Step 10 prelaunch recovery/integrity gate and keep all neural held-out values embargoed. | The corrected smoke repeated a deliberate epoch-checkpoint interruption, resumed under one immutable code identity, cleared stale failure state, sealed all 14 configurations, and passed independent score-blind recomputation and coverage checks with zero warnings. The earlier v1 artifact is retired because its audit/status executable changed. | Commits `f7fea86`, `81230ca`; AWS `step10_neural_full_smoke_v2/integrity_audit.json`. |
| 2026-08-07 | Launch `step10_neural_full_v1` as the primary 18-site neural evaluation without reducing the frozen workload. | All unit, real-data GPU, recovery, artifact, notification, source-hash, storage, and score-blind audit gates passed. Smoke timing is not used to weaken the protocol because its two-epoch override is not representative of nested early-stopped training. | Code version `919b359`; AWS start time `2026-08-07T05:48:42Z`; STARTED SNS publish succeeded. |
| 2026-08-07 | Permit development of a three-worker site-parallel execution layer while leaving the accepted single-worker run active and isolated. | Outer-site evaluations are independent, so scheduling can be parallelized without changing the scientific estimand. Replacement is conditional on score-blind equivalence, crash recovery, provenance audit, at least `1.5x` measured speedup, and a favorable break-even calculation. No artifact from differently versioned executions may be mixed. | `configs/neural_parallel_execution.json`; `docs/step10_parallel_execution.md`. |
| 2026-08-07 | Retire `step10_neural_full_v1` and replace it with a fresh three-worker full execution rather than mixing partial single-worker artifacts. | All parallel gates passed, outputs were exactly equivalent, measured smoke speedup was 1.63x, and only about 9.9% of fit checkpoints had been started in the original run. A restart was projected to save roughly 12-15 hours. Graceful SIGTERM preserved the original archive and published an alert. | User authorization in the continuation chat; retired status at 2026-08-07T10:15:39Z; `docs/step10_parallel_execution.md`. |
| 2026-08-07 | Launch `step10_neural_full_parallel_v1` as the new primary full neural execution under commit `c246744`. | A clean commit-labelled directory passed 55 tests, byte-identical critical hashes, CUDA/storage/process checks, private alert configuration, and a new-run-ID check. Three workers received disjoint six-site assignments and the STARTED alert published before fitting. | AWS start at 2026-08-07T10:16:57Z; SNS message ID retained in the run notification artifact. |
| 2026-08-08 | Accept the completed `step10_neural_full_parallel_v1` artifact for Step 11 only after a score-blind integrity audit and byte-verified backup. | The independent auditor validated all 18 sites, three disjoint worker roots, canonical copies, hashes, 52,780 prediction rows, 1,260 recomputed hidden metric rows, 158,340 diagnostic rows, 9,324 runtime rows, and zero warnings without reporting values. The local sealed archive exactly matches AWS. | Integrity certificate SHA-256 `19b61c64...0960`; sealed archive SHA-256 `91ffbd55...5082f`; `docs/step11_confirmatory_analysis.md`. |
| 2026-08-08 | Freeze the Step 11 execution contract, statistics, output templates, and unblinding guard before reading accepted neural results. | The original run-time analysis protocol fixed the estimand class; this post-audit/pre-unblinding contract makes the numerical procedure fully executable: anchored normalized density-curve area, five-seed-within-site averaging, equal-site inference, 10,000 paired bootstraps, exact sign-flip density tests with within-family Holm correction, site-aggregated gauge-aware diagnostics, and the pre-specified three-condition decision rule. | `configs/neural_confirmatory_analysis_v1.json`; `scripts/analyze_neural_full.py`; `tests/test_analyze_neural_full.py`; 4/4 synthetic analysis tests and 59/59 complete AWS tests passed before unblinding. |
| 2026-08-08 | Classify the Step 11 core result as no complete transfer of the proposed BuNN anti-collapse advantage under the frozen ABIDE-I pipeline. | The primary BuNN-minus-GCN predictive interval crossed zero and excluded a +0.03 practical advantage; BuNN was below the connectome elastic-net reference; and the matched-anchor primary effective-rank change favored GCN rather than BuNN. None of the three pre-specified positive-result conditions passed. | `docs/step11_neural_results.md`; analysis archive SHA-256 `d03235fd...5405`; pre-unblinding commit `6e1716e`. |
| 2026-08-09 | Freeze Step 12A as a secondary influence/robustness audit that cannot modify or override Step 11. | The exhaustive scope was fixed before robustness execution: all 18 leave-one-site exclusions for three core contrasts, all five seeds for two curve contrasts, three alternative summaries, nine pre-listed curve comparisons, and operator efficiency. Point-estimate sign changes define site/seed sensitivity; no excluded site or seed may become primary. | `configs/neural_robustness_analysis_v1.json`; `docs/step12_robustness_plan.md`; `scripts/analyze_neural_robustness.py`. |
| 2026-08-09 | Classify the Step 11 null as site- and seed-sensitive for BuNN versus GCN, while retaining robust negative baseline and matched-anchor representation findings. | Excluding CALTECH changed the curve point estimate to +0.00045 but its interval crossed zero; two of five seed point estimates favored BuNN but neither favorable interval excluded zero. By contrast, BuNN remained below elastic net and its effective-rank change remained below GCN under all 18 exclusions. | `docs/step12_robustness_results.md`; Step 12 archive SHA-256 `c29ea30f...1be0c`. |
| 2026-08-15 | Add one post-hoc nonlinear non-graph comparator without reopening Study 1. | An RBF-SVM on the same 6,670 connectome features tests whether a nonlinear vectorized model changes the practical baseline. The cohort, outer and inner site splits, scaling boundary, fixed zero threshold, nine-candidate grid, and score-blind audit were frozen before fitting. The result cannot override the confirmatory decision. | `configs/extensions/nonlinear_connectome_baseline_v1.json`; `docs/extensions/nonlinear_connectome_baseline_plan.md`. |
| 2026-08-15 | Promote the RBF-SVM comparator after the one-site timing gate. | The CALTECH engineering smoke completed and passed its score-blind audit in 8.44 minutes locally. A simple 18-site sequential projection was 2.53 hours, below the frozen 16-hour gate. No held-out metric was opened. | Run `rbf_svm_caltech_timing_smoke_v1`; `outputs/runs/nonlinear_baseline` is private and Git-ignored. |
| 2026-08-09 | Freeze Step 13 paper evidence to the audited Step 7, 11, and 12 packages before drafting prose. | A claim-sensitive negative-result paper must not acquire manually copied values, favorable post hoc summaries, or silent evidence drift. The paper builder validates 28 source/archive hashes, emits a deterministic result snapshot and artifact inventory, and limits the manuscript to the claim ledger. | `configs/paper_assets_v1.json`; `docs/step13_claim_ledger.md`; `scripts/build_paper_assets.py`; evidence commit `a4def2a`. |
| 2026-08-09 | Replot publication figures from the same frozen site-level values rather than publishing the archived analysis-layout images unchanged. | Visual QA found overlapping 0%/1% labels and raw operator names. The replacement plots alter only labels, layout, markers, and resolution; they preserve the accepted site-level values and add no inferential test. | `paper/generated/figures`; `paper/generated/paper_asset_manifest.json`; deterministic rebuild and visual QA passed. |
| 2026-08-09 | Bind Step 13.2 prose and tables to the frozen evidence snapshot and move large secondary site/seed diagnostics to an S-numbered supplement. | A negative-result manuscript is vulnerable to transcription drift and selective presentation. Generated macros/tables prevent manually duplicated headline values; keeping the secondary plots in the supplement preserves complete reporting while maintaining a clean main Results-to-Discussion sequence. | `configs/manuscript_v1.json`; `scripts/build_manuscript_inputs.py`; `paper/manuscript.tex`; `paper/supplement.tex`; `output/pdf`. |
| 2026-08-09 | Replace the card-style study workflow with restrained line art and add one operator schematic, while leaving all frozen evidence and inferential outputs unchanged. | The original pastel boxes resembled a generic presentation template and did not explain the central distinction among GCN, learned-local, and learned-BuNN operations. Decorative brain imagery and unsupported attribution maps were excluded. | `scripts/build_manuscript_inputs.py`; `paper/generated/figures/study_design.png`; `paper/generated/figures/operator_schematic.png`; final PDF visual QA. |
| 2026-08-10 | Restyle both explanatory schematics to match the empirical figures rather than using an independent infographic theme. | A tinted field, editorial banner, and separate accent palette made the schematics look imported from a presentation. The final versions use the paper's white background, standard panel labels, and the existing GCN blue, trivial-bundle orange, and BuNN green. | `paper/generated/figures/study_design.png`; `paper/generated/figures/operator_schematic.png`; rendered manuscript pages 2 and 3. |
| 2026-08-10 | Remove the general study-design diagram from the manuscript while retaining the operator schematic and the generated design asset. | The diagram repeated the Methods and cohort table without adding a scientific distinction. The operator schematic remains because it explains the central architectural comparison. The removed asset may still support a poster or oral presentation. | `paper/sections/03_methods.tex`; rebuilt 8-page manuscript; complete page-level visual QA. |
| 2026-08-10 | Separate the public reproducibility archive from the private participant-linked audit record. | The frozen artifact manifest must remain hash-bound for provenance, but redistributing raw data, predictions, checkpoints, cloud settings, or the 1 GB sealed training archive is unnecessary and increases privacy and security risk. The public package therefore contains code, aggregate evidence, manifests, paper assets, and PDFs while retaining only hashes for excluded evidence. | `configs/release_v1.json`; `scripts/build_release_package.py`; `docs/release.md`; deterministic release tests and privacy scan. |
| 2026-08-10 | Enforce LF endings for versioned text and mark research/publication artifacts as binary. | The first clean Windows clone converted a hash-bound Markdown input to CRLF, producing a false digest mismatch before manuscript generation. A repository-level line-ending contract keeps text bytes stable across Windows and Linux while preventing conversion of PDFs, figures, archives, and arrays. | `.gitattributes`; failed clean audit at commit `5d6f918`; repeated clean-checkout gate required. |
| 2026-08-10 | Mark three Step 13.1 evidence-regeneration tests as conditional on the private evidence archives. | A public clone intentionally excludes participant-linked outputs and the sealed neural archive, so those tests cannot rebuild paper assets from the original analysis rows. They continue to run in the private repository, while the public clone explicitly skips them and still tests the frozen snapshot, manuscript, claims, release safety, and determinism. | `tests/test_build_paper_assets.py`; clean audit at commit `ec9af85`; `docs/release.md`. |
| 2026-08-10 | Require the release builder to package only committed files from a clean tracked tree. | LaTeX compilation rewrites PDF metadata timestamps. Without a clean-tree guard, an archive could name one source commit while containing locally modified PDFs. The compiler output proves reproducibility, while the release archive must remain an exact snapshot of HEAD. | `scripts/build_release_package.py`; `tests/test_build_release_package.py`; clean compilation of commit `3bdf2ff`. |
| 2026-08-14 | Fail the ABIDE-II external-evaluation gate before model fitting or prediction. | The official main phenotype and quality records are available, but no complete official C-PAC `filt_noglobal` AAL-116 ROI-time-series derivative maps to the 1,114-person main cohort. The LLE area uses a different mixed C-PAC/SPM volume workflow, and all 38 longitudinal identifiers overlap ABIDE-I. Treating either source as the frozen Study 1 representation would make the external comparison ambiguous. | `configs/abide_ii_inventory_v1.json`; `reproducibility/abide_ii_gate_inventory.json`; `docs/abide_ii_compatibility_gate.md`; four inventory tests passed. |
