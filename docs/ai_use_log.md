# AI assistance log

This is a local, version-controlled provenance record for AI-assisted work on
this project. It records assistance, not authorship: every technical claim,
implementation decision, experiment, and conclusion must be independently
reviewed and approved by the researcher.

## Maintenance rule

Codex will append an entry after each AI-assisted task it performs for this
repository. Assistance from tools Codex cannot observe (for example Cursor,
Claude, or another chat) must be supplied by the researcher with its date,
tool, and purpose before it can be logged accurately.

## Entries

| Date | Tool | Assistance provided | Researcher verification required | Evidence |
| --- | --- | --- | --- | --- |
| 2026-07-31 | Codex | Audited and narrowed the submitted BuNN/connectome proposal into an ABIDE-I operator-comparison study; identified claim and evaluation risks. | Verify all literature, protocol choices, and final scientific wording. | Prior Codex research-planning chats retained by the researcher. |
| 2026-08-03 | Codex | Produced an implementation plan; created the private repository and initial reproducibility scaffold. | Review the proposed project structure and decide the final protocol before experiments begin. | Git commit `6479bad`. |
| 2026-08-03 | Codex | Guided AWS EC2 GPU instance setup and SSH troubleshooting for the BuNN experiment environment. | Confirm the final active instance configuration, cost controls, and that credentials are kept private. | User confirmed SSH access to Ubuntu instance succeeded. |
| 2026-08-03 | Codex | Built and tested the ABIDE-I PCP manifest tool; compared QC subsets, verified all selected derivative URLs, and recorded the frozen primary cohort and exclusions. | Review the primary QC/site-eligibility decision before beginning data download and model training. | `scripts/prepare_abide_manifest.py`; `docs/step5_abide_manifest.md`; `configs/abide_i_primary_manifest.json`. |
| 2026-08-03 | Codex | Connected to the user-authorized AWS instance, checked storage and GPU-driver readiness, created a resumable public ABIDE-I ROI time-series downloader, and installed/validated the frozen cohort. | Confirm the retained AWS download summary before analysis; install and verify the NVIDIA driver before training. | `scripts/download_abide_timeseries.py`; 769 manifest-matched files, 0 malformed files. |
| 2026-08-03 | Codex | Installed the Ubuntu AWS NVIDIA driver/kernel module, rebooted the authorized instance, created an isolated PyTorch environment, and verified CUDA computation on the A10G. | Re-run the documented health checks if the instance image, kernel, or driver changes. | `docs/aws_environment.md`; `nvidia-smi`; PyTorch CUDA matrix-multiplication test. |
| 2026-08-03 | Codex | Implemented and executed ABIDE-I technical QC and Fisher-z connectome construction; identified 15 zero-variance-ROI files, created a separate analysis manifest, and validated the stored artifacts. | Review the technical eligibility rule and exclusions before interpreting or modeling the data. | `scripts/build_abide_connectomes.py`; `scripts/filter_abide_technical_qc.py`; `docs/step6_connectomes.md`. |
| 2026-08-03 | Codex | Froze the Step 7 baseline protocol, built the aligned baseline table, generated grouped site-held-out assignments, and independently audited their leakage and reproducibility properties. | Review the frozen protocol before fitting any baseline; no performance results exist yet. | `configs/baseline_protocol.json`; `configs/baseline_inputs_and_splits.json`; `docs/step7_baselines.md`. |
| 2026-08-03 | Codex | Implemented, tested, and ran the Step 7.3 baseline runner and Step 7.4 AWS engineering smoke test; recorded two implementation/runtime failures, their fixes, the smoke override, and artifact audit. | Treat the smoke artifacts as engineering evidence only. Run the full 18-site baseline evaluation without `--fast-smoke` before making predictive claims. | Commits `7dab13f`, `60c9bef`, `4654ee9`, `cd0bee8`; `scripts/run_baselines.py`; `docs/step7_baselines.md`; `outputs/runs/baselines/step7_4_fast_smoke_nyu_caltech/`. |
| 2026-08-03 | Codex | Implemented and AWS-tested atomic per-site checkpoints, immutable-contract resume, live status reading, detached launch control, and optional SNS notification hooks. | SNS creation/publish was blocked because the EC2 instance role supplied no usable credentials. Attach a least-privilege SNS role and confirm one emailed test alert before a full run. | Commit `a45e924`; `docs/step7_run_control.md`; run `checkpoint_resume_smoke`. |
| 2026-08-03 | Codex | Verified the user-created least-privilege EC2 SNS role, published a harmless test alert, configured the ignored owner-only alert file, and made managed runs fail closed when the alert configuration is absent. | Researcher confirmed that the test email reached the subscribed inbox. | Commit `7fdcf04`; `scripts/test_sns_alert.py`; `scripts/launch_managed_baseline.sh`; `docs/step7_run_control.md`. |
| 2026-08-05 | Codex | Reconciled the local repository, scientific protocol, and prior AWS handoff before continuation. | The prior public SSH endpoint did not respond; verify the instance's current state and public address in EC2 before any new remote action. | Local Git history through `7fdcf04`; current continuation chat. |
| 2026-08-05 | Codex | With the researcher's explicit authorization, added two SSH-only `/32` security-group rules: one for the researcher's current browser network and one for the authorized execution environment. Reconnected, completed the AWS preflight, normalized an ignored alert-file line-ending issue, and published one SNS test alert. | Confirm the test email arrives. No model-training process was started. | EC2 security group `sg-0f43fe350c37bbcfb`; `nvidia-smi`; PyTorch CUDA smoke; `scripts/check_baseline_run.py`; SNS message ID retained in the continuation chat. |
| 2026-08-05 | Codex | Audited the frozen Step 7.5 workload and existing AWS recovery/alert evidence, then wrote the prelaunch, monitoring, recovery, integrity, post-baseline, neural-model, statistical-analysis, and paper execution plans. | Review and explicitly authorize the Step 7.5 contract before launch; the estimates and later neural design remain plans until implemented and tested. | `docs/step7_5_full_baseline_plan.md`; `docs/research_execution_plan.md`. |
| 2026-08-05 | Codex | With explicit authorization, repeated AWS preflight, attempted the full Step 7.5 launch, and diagnosed a pre-fit SNS `NoRegionError`. Implemented a topic-ARN-derived SNS region fix, added a regression test, deployed it to AWS, and ran the intended test suite successfully. | `v1` produced no fit, prediction, or metric and must not be interpreted or resumed. Confirm the separately identified `v2` run starts before treating any work as active. | Commit `7ad7246`; AWS run `step7_5_full_baselines_v1`; `tests/test_run_baselines.py` (13 passed on AWS). |
| 2026-08-05 | Codex | Launched the authorized full 18-site Step 7.5 baseline evaluation after preflight and confirmed the detached runner, required STARTED alert, and initial full-grid tuning state. | Do not inspect test metrics until the documented completion integrity audit passes. On a terminal email, re-check status, manifests, hashes, and row coverage before analysis. | AWS run `step7_5_full_baselines_v2`; started `2026-08-05T08:01:22Z`; status message ID retained in the continuation chat. |
| 2026-08-06 | Codex | With explicit authorization, added a single SSH-only temporary `/32` inbound rule for the current execution network after its public egress IP changed, restoring read-only monitoring of the active baseline run. Confirmed AWS instance, system, and EBS health; checked the live runner and checkpoint status without opening predictive metrics. | Remove stale temporary SSH `/32` rules once convenient; keep the completion/results embargo in force until the integrity audit passes. | Security group `sg-0f43fe350c37bbcfb`; added TCP/22 source `49.36.45.195/32`; run `step7_5_full_baselines_v2` status at `2026-08-06T02:59:12Z`. |
| 2026-08-06 | Codex | With explicit authorization, added a second SSH-only temporary `/32` rule after the execution environment's egress IP changed again. Reconnected and read the live checkpoint without opening predictive metrics. | Remove stale temporary SSH `/32` rules once convenient; keep the completion/results embargo in force until the integrity audit passes. | Security group `sg-0f43fe350c37bbcfb`; added TCP/22 source `49.36.44.105/32`; status at `2026-08-06T16:18:02Z`: 13 of 18 sites sealed, UCLA_2 active. |
| 2026-08-06 | Codex | Counted only sealed prediction-file rows and fold-completion markers to report participant coverage; no prediction values or performance metrics were opened. | Keep the results embargo in force until the full completion integrity audit passes. | Run `step7_5_full_baselines_v2`: 13 sealed folds, 489 of 754 held-out participants sealed, 265 remaining; each sealed fold had exactly three prediction rows per participant. |
| 2026-08-07 | Codex | With explicit authorization, added a third temporary `/32` SSH rule after the execution environment's egress IP changed. Verified the terminal run status and aggregate row counts without viewing prediction values or performance metrics. | Perform the independent integrity audit described in `docs/step7_5_full_baseline_plan.md` before scientific analysis. Remove stale temporary SSH `/32` rules when convenient. | Security group `sg-0f43fe350c37bbcfb`; added TCP/22 source `89.33.8.59/32`; run status `complete` at `2026-08-07T01:56:53Z`; 18 fold completion markers, 2,262 prediction rows, 54 metric rows, 396 tuning rows, and 6,732 inner-site rows. |
| 2026-08-07 | Codex | Implemented a score-blind independent auditor, tested it on synthetic valid/corrupted runs, fixed a direct-script import-path issue, and ran it against the completed 18-site AWS baseline archive. Copied the verified run archive and score-blind certificate locally with matching SHA-256 checks. | Review the audit code and archive hashes before any unblinding. Implement and test Step 7.6 analysis on synthetic data before reading performance values. | `scripts/audit_baseline_run.py`; `tests/test_audit_baseline_run.py`; AWS audit report `outputs/audits/step7_5_full_baselines_v2.integrity.json`; local archive SHA-256 `bc8fd504...e4476`. |
| 2026-08-07 | Codex | Implemented the fixed Step 7.6 analysis module and its synthetic-data tests, including participant-level metric recomputation, runner-metric consistency checks, paired site bootstrap, figure/table outputs, and an explicit unblinding acknowledgement guard. | Review the frozen analysis contract and implementation commit before authorizing the first read of real predictive results. | `scripts/analyze_baselines.py`; `tests/test_analyze_baselines.py`; `docs/step7_6_baseline_analysis_plan.md`; 17 AWS tests passed. |
| 2026-08-07 | Codex | Ran the acknowledged Step 7.6 command once; it stopped at a selected-candidate CSV-flag compatibility error before writing outputs or printing performance values. Corrected the parser from the runner's existing `0`/`1` writer contract and repeated the unchanged AWS test suite. | Review the compatibility-fix commit before accepting the rerun's analysis outputs. | `scripts/run_baselines.py:588`; `scripts/analyze_baselines.py`; 17 AWS tests passed after the correction. |
| 2026-08-07 | Codex | Reran the acknowledged frozen Step 7.6 analysis after the compatibility fix, checked that the analysis outputs were generated, verified the local archive hash against AWS, and visually inspected the two generated figures for readability. | Independently review the baseline result interpretation and preserve the archived outputs before beginning neural work. | AWS `outputs/analysis/step7_6_full_baselines_v2`; local archive SHA-256 `c60d4f...3267a`; `docs/step7_6_baseline_results.md`. |
| 2026-08-07 | Codex | Implemented the Phase 8 neural graph-data layer, matched identity/GCN/trivial-bundle/learned-BuNN propagation operators, common-frame diagnostics, contract config, synthetic tests, real-data input check, and GPU-only forward/backward smoke. | Review the fixed operator semantics and implementation before a fold-aware runner or any neural training is started. | `src/neural_data.py`; `src/neural_operators.py`; `src/neural_models.py`; `configs/neural_operator_contract.json`; `docs/step8_neural_implementation.md`; 13 Phase 8 synthetic tests, 20 real-data GPU smoke cells, and 34 complete-suite tests passed. |
| 2026-08-07 | Codex | Implemented and AWS-tested the score-blind neural engineering-pilot runner, managed launcher, atomic per-epoch recovery, resume-contract checks, status/SNS reporting, and independent pilot auditor. Corrected two deployment/recovery defects before accepting the clean v2 artifact; launched an intentional checkpoint interruption and resumed it under unchanged code. | Treat `step9_neural_pilot_v2` solely as engineering evidence. Review the pilot audit and freeze the complete neural evaluation protocol before any held-out-site neural prediction is run. | Commits `5fb3838`, `727c557`, `22b2ec2`; `scripts/run_neural_pilot.py`; `scripts/audit_neural_pilot.py`; `docs/step9_neural_pilot.md`; 20 cells/20 checkpoints and 60 loss-only records audited on AWS. |
| 2026-08-07 | Codex | Added the learned-local same-capacity control; froze the five-operator full neural and analysis contracts; implemented nested training, managed recovery, score-blind auditing, and regression tests; and exercised every operator on real ABIDE-I tensors and the A10G. | Review the operator/control semantics, tuning/refit rule, workload, and analysis estimands before accepting the full run. No smoke predictive value was inspected. | Commits `f7fea86`, `81230ca`; `configs/neural_*`; `src/neural_full_training.py`; `scripts/run_neural_full.py`; `scripts/audit_neural_full.py`; 50 AWS tests passed. |
| 2026-08-07 | Codex | Ran two reduced full-run engineering attempts. The first completed but exposed an order-sensitive auditor comparison after JSON key sorting. Corrected that audit defect and stale recovery-status fields, then retired v1 and repeated the intentional checkpoint interruption/resume under a fresh v2 run. The v2 score-blind audit passed all hashes, schemas, coverage, tuning, metric-recomputation, diagnostic, and runtime checks with zero warnings. | Treat both runs as engineering evidence only. Preserve the results embargo until the 18-site run completes and passes the same independent audit. | AWS runs `step10_neural_full_smoke_v1` and `step10_neural_full_smoke_v2`; v2 audit checked 14 configurations, 42 prediction rows, 14 hidden metric rows, 126 diagnostic rows, 28 tuning/runtime rows, and zero warnings. |
| 2026-08-07 | Codex | Repeated the full AWS preflight, verified byte-identical critical source/configuration hashes, idle A10G and sufficient storage, confirmed the accepted smoke alert was published, and launched the frozen 18-site neural evaluation as a detached managed run. | Do not inspect held-out neural values until all 18 sites seal and the independent score-blind audit passes. On any terminal alert, verify status, process, hashes, site seals, and audit output before analysis. | AWS run `step10_neural_full_v1`; PID `58303`; start SNS message ID retained in `notification.json`; code version `919b359`. |
| 2026-08-07 | Codex | Began an execution-only parallel extension without stopping the active run. Added a frozen three-worker contract, internal site-shard mode, isolated coordinator/workers, recovery, canonical hash-verified merge, parallel provenance auditing, and score-blind sequential-equivalence checking. Deployed only to a separate AWS development directory and ran the complete unit suite. | Do not replace or stop the active single-worker run until real-data recovery, equivalence, audit, speed, and break-even gates pass. | `configs/neural_parallel_execution.json`; `scripts/run_neural_full_parallel.py`; `scripts/compare_neural_execution_equivalence.py`; `docs/step10_parallel_execution.md`; 55 AWS tests passed. |
| 2026-08-07 | Codex | Ran clean, intentional-failure/resume, and sequential-control real-data smokes for the three-worker extension. Verified that unaffected workers complete after one worker stops, canonical merge waits for complete coverage, resume preserves sealed sites, parallel provenance audits pass, and all scientific outputs match sequential execution exactly. | Decide whether the measured speedup justifies retiring and restarting the active full run. No held-out predictive or representation value was displayed. | AWS development runs `step10_parallel_clean_smoke_v1`, `step10_parallel_recovery_smoke_v1`, and `step10_sequential_equivalence_smoke_v1`; 1.63x smoke speedup; zero numerical difference; zero audit warnings. |
| 2026-08-07 | Codex | With explicit authorization, captured a score-blind checkpoint, gracefully stopped the original full runner, verified its interrupted state and SNS alert, preserved its hashes/artifacts, deployed the validated parallel code to a clean commit-labelled AWS directory, repeated 55 tests and all preflight checks, and launched the fresh three-worker full run. | Keep held-out results embargoed. On terminal alert, validate all worker roots, 18 canonical site copies, hashes, counts, and hidden metric recomputation before analysis. | Retired AWS run `step10_neural_full_v1`; active replacement `step10_neural_full_parallel_v1`; code `c246744`; STARTED alert and three disjoint worker assignments verified. |
| 2026-08-08 | Codex | Verified full neural completion, ran the independent score-blind audit, created a file-level SHA-256 inventory and sealed archive, downloaded it locally, and confirmed an exact AWS/local archive hash match. | Preserve the archive and integrity certificate; do not read accepted neural values until the Step 11 implementation and templates are frozen. | `step10_neural_full_parallel_v1`; 18/18 sites; 52,780 predictions; 1,260 hidden metrics; 158,340 diagnostics; zero warnings; archive SHA-256 `91ffbd55...5082f`. |
| 2026-08-08 | Codex | Implemented the guarded Step 11 confirmatory analysis and a synthetic 18-site test fixture without reading accepted neural values. Fixed anchored density-curve estimands, paired site bootstrap, exact sign-flip/Holm secondary tests, gauge-aware site aggregation, decision rules, tables, figures, and manifests. | Review the frozen contract and code commit before the single acknowledged unblinding invocation. | `configs/neural_confirmatory_analysis_v1.json`; `scripts/analyze_neural_full.py`; `tests/test_analyze_neural_full.py`; 4 new tests and 59 complete AWS tests passed. |
| 2026-08-08 | Codex | Committed the pre-unblinding analysis state, verified byte-identical AWS deployment, performed the single acknowledged unsealing invocation, verified all generated hashes, copied the analysis archive locally, inspected the fixed tables and all three figures, and applied the frozen decision rule. | Independently review the negative/null interpretation and decide whether to run the separately labelled Step 12 influence/robustness analyses before paper drafting. | Pre-unblinding commit `6e1716e`; analysis archive SHA-256 `d03235fd...5405`; `docs/step11_neural_results.md`. |
| 2026-08-09 | Codex | Implemented the frozen Step 12A robustness contract and a lightweight standalone analyzer for leave-one-site influence, all-seed stability, alternative summaries, exhaustive curve comparisons, efficiency, fixed classification, figures, and hashes. Built a synthetic 18-site fixture and exercised both analysis and real figure rendering without reading raw training artifacts. | Review the contract and pre-execution commit; then run once against the immutable Step 11 package. The Step 11 conclusion cannot be overwritten. | `configs/neural_robustness_analysis_v1.json`; `scripts/analyze_neural_robustness.py`; `tests/test_analyze_neural_robustness.py`; 4/4 tests and synthetic render smoke passed locally. |
| 2026-08-09 | Codex | Ran Step 12A once against the hash-bound Step 11 package, verified all generated hashes, archived the outputs, visually inspected both fixed figures, applied the frozen sensitivity classification, and documented every complete contrast family rather than selecting a favorable exclusion or seed. | Review the mixed-sensitivity interpretation before paper drafting; retain Step 11 as confirmatory. | `docs/step12_robustness_results.md`; archive SHA-256 `c29ea30f...1be0c`; robustness category `mixed_site_and_seed_sensitive_null`. |
| 2026-08-09 | Codex | Implemented Steps 13.0--13.1: froze 28 paper-evidence hashes, created a deterministic result snapshot and artifact inventory, wrote a claim ledger, generated compact paper tables, and replotted four publication-layout figures from the accepted site-level values. Visual QA caught and corrected overlapping density labels and raw operator names; the complete local suite passed 67 tests. | Keep manuscript numbers tied to the generated snapshot/tables. Do not interpret presentation-only replotting as a new analysis. Review authorship and final prose before external release. | `configs/paper_assets_v1.json`; `scripts/build_paper_assets.py`; `tests/test_build_paper_assets.py`; `docs/step13_claim_ledger.md`; `paper/generated`; `reproducibility`. |
| 2026-08-09 | Codex | Implemented Step 13.2: created the hash-bound manuscript-input contract and deterministic builder, drafted the LaTeX main paper and supplement from the approved claim ledger, verified primary references, added integrity tests, compiled both PDFs, and visually inspected every page. Visual QA moved secondary robustness figures to the supplement and removed wasteful float-only pages. A transient OneDrive file lock was fixed with bounded atomic-write retry. | Read and revise the manuscript in the researcher's own voice, verify every scientific statement and author/affiliation detail, and adapt it to a chosen venue before external release. No new model fitting or inference occurred. | `configs/manuscript_v1.json`; `scripts/build_manuscript_inputs.py`; `tests/test_build_manuscript_inputs.py`; `paper`; `output/pdf`; 62 complete repository tests passed. |

| 2026-08-09 | Codex with project-local Humanizer guidance | Rewrote the manuscript, supplement, captions, title, and diagram labels in a more natural academic voice without changing results or claim boundaries. Replaced the pastel card-style workflow with abstract line art, added a computational operator schematic, corrected float ordering, rebuilt both PDFs, and visually reviewed every page. | Review the final wording and both new schematics. The operator drawing is explanatory and must not be interpreted as biological signal flow. | `paper`; `scripts/build_manuscript_inputs.py`; `paper/generated/figures`; `output/pdf`. |

| 2026-08-10 | Codex | Restyled the study-design and operator schematics after researcher feedback so they match the paper's empirical figures. Removed the tinted background, infographic heading, accent theme, and dividers; reused the established operator colors and visually checked the rebuilt manuscript pages. | Confirm that the final white-background figures match the intended publication style. No scientific values or interpretations changed. | `scripts/build_manuscript_inputs.py`; `paper/generated/figures`; `output/pdf/manuscript.pdf`. |

| 2026-08-10 | Codex | Removed the general study-design diagram and its cross-reference from the main manuscript, retained the operator schematic, rebuilt the PDF, and checked all eight pages after automatic figure renumbering. | Confirm the tighter Methods flow and retain the unused study-design asset only if it is useful for later presentation material. | `paper/sections/03_methods.tex`; `output/pdf/manuscript.pdf`. |

| 2026-08-10 | Codex with project-local Humanizer, LaTeX, and PDF workflows | Audited the final manuscript prose and metadata, synchronized stale project status records, created the public/private release boundary, added pinned direct dependencies and citation metadata, replaced a real AWS account identifier in a test fixture, implemented a deterministic privacy-scanned release builder, and completed clean reproduction and visual checks. | Verify the displayed author name and affiliation and choose the final software license before external publication. No model fitting or new statistical analysis occurred. | `README.md`; `docs/release.md`; `configs/release_v1.json`; `scripts/build_release_package.py`; `requirements.txt`; `CITATION.cff`; complete test, compile, and PDF QA records. |

| 2026-08-10 | Codex | Investigated a failed clean-checkout manuscript build caused solely by Windows CRLF conversion of a hash-bound Markdown file, then added a cross-platform Git line-ending contract before repeating the audit. | Confirm that future contributors do not override `.gitattributes` when regenerating frozen evidence. | `.gitattributes`; clean-checkout failure at commit `5d6f918`; `docs/decision_log.md`. |

| 2026-08-10 | Codex | Investigated three clean-clone failures caused by intentionally excluded private analysis archives. Made only the evidence-regeneration tests conditional on those archives and documented the public/private test counts; the frozen snapshot and manuscript tests remain mandatory. | Keep the private 75-test audit and public 72-test plus 3-skip audit distinct when reporting reproducibility. | `tests/test_build_paper_assets.py`; `docs/release.md`; clean-checkout failure at commit `ec9af85`. |

| 2026-08-10 | Codex with bundled LaTeX and PDF workflows | Completed the clean-checkout audit at commit `3bdf2ff`: rebuilt manuscript inputs, ran the public test suite, resolved the dependency lock, compiled the main paper with BibTeX and the supplement, extracted text, rendered all 11 pages, and inspected them. Added a clean-tree release guard after observing that recompilation changes PDF metadata timestamps. | Verify author identity and affiliation before any external release. The visual and technical audit found no document defect. | `docs/release.md`; `scripts/build_release_package.py`; clean audit clone `bunn-abide-clean-audit-3bdf2ff`. |

| 2026-08-12 | Codex with bundled LaTeX and PDF workflows | Converted the main manuscript to the IEEE conference layout with two columns, IEEE title and author blocks, numbered citations, IEEE bibliography styling, keywords, and column-aware figures and tables. Reformatted the supplement in a compact one-column IEEE supplemental layout, regenerated all evidence-bound paper inputs, compiled both PDFs, and visually inspected every page. | Verify the displayed author and affiliation and select a specific venue template before submission; this is a clean IEEE-style working format, not a claim of compliance with every venue-specific page or anonymity rule. No scientific result or interpretation changed. | `paper`; `scripts/build_manuscript_inputs.py`; `output/pdf/manuscript.pdf`; `output/pdf/supplement.pdf`; five manuscript-input tests passed. |

| 2026-08-13 | Codex | Verified the official BuNN, Wang cGCN, released Wang code, and ABIDE-II sources; then wrote an implementation-ready extension plan covering competing explanations of the completed null result, checkpoint perturbations, ground-truth synthetic geometry, matched depth/capacity controls, a leakage-free Wang model ladder, and prospective ABIDE-II validation. | Review and freeze the extension registry before implementation. This entry records planning only: no extension training, held-out inference, or new scientific result occurred. | `docs/posthoc_mechanism_external_extension_plan.md`; official ICLR proceedings, Wang paper/repository, and ABIDE-II documentation checked on 2026-08-13. |

| 2026-08-13 | Codex with bundled PDF workflow | Rendered and inspected every page of the original and updated research-outline PDFs, compared each extension phase with their exact scientific promises and the current manuscript, verified that the sealed Study 1 archive contains the checkpoints and training histories needed for the proposed audit, restored explicit orthogonality/bundle-dimension/sign-sensitivity planning, and classified what belongs in the main paper, supplement, or a separate Wang benchmark. | Review the scope classification before freezing the extension registry. This was a document/artifact audit only; no new model result was produced. | `docs/outline_alignment_and_paper_scope_audit.md`; `docs/posthoc_mechanism_external_extension_plan.md`; source PDF hashes recorded in the alignment audit. |

| 2026-08-13 | Codex | Added a direct three-arm Wang outer-test-leakage audit (released-code, grouped-inner leakage-free, and fixed-epoch), ranked the extension studies by explanatory value, and produced an evidence-based implementation/compute/paper timeline anchored to the archived Study 1 runtime. | Replace provisional Wang and ABIDE-II runtime ranges with measured one-site/10-participant smoke results before authorizing full compute. No new training or inference occurred. | `docs/posthoc_mechanism_external_extension_plan.md`; `docs/extension_priority_and_timeline.md`; Study 1 runtime summary and coordinator timestamps. |
| 2026-08-13 | Codex | Audited checkpoint availability and designed a 96-hour parallel AWS relay plan prioritizing accepted-checkpoint interventions, synthetic geometry controls, and the three-arm Wang leakage audit. The plan keeps all Wang arms for a site together and uses freed machines as whole-site shards after a measured smoke. | Confirm the on-demand G-instance vCPU quota and freeze the extension registry before provisioning. Four-day Wang completion remains conditional on the measured hour-48 forecast. No compute was launched. | `docs/four_day_parallel_extension_sprint.md`; sealed Study 1 archive contains 9,324 checkpoint files. |
| 2026-08-13 | Codex | Re-scoped the extension sprint to one existing `g5.xlarge` and 72 hours. The revised plan completes accepted-checkpoint interventions first, then uses a score-blind Wang timing/concurrency smoke to choose between a full 17-site three-arm leakage audit and a compact synthetic mechanism study. | Freeze the extension contract before execution. The full Wang audit is authorized only if measured runtime plus contingency leaves eight hours for audit and paper QA; no new compute was launched. | `docs/seventy_two_hour_single_instance_sprint.md`. |
| 2026-08-13 | Codex | Audited the proposed 72-hour schedule against the repository, sealed checkpoint archive, measured Study 1 runtimes, released Wang code/dependencies, public LOSO data link, and current SSH reachability. Rejected the provisional full-Wang/synthetic branch and retained only the existing-artifact plus checkpoint-intervention audit as a quality-supported 72-hour extension. | Restore SSH before starting. Require a complete one-site intervention timing smoke and preserve at least 12 hours for manuscript QA. Wang, synthetic, depth, and ABIDE-II results are deferred rather than rushed. No scientific run occurred. | `docs/seventy_two_hour_feasibility_audit.md`; 360 selected checkpoints totaling 45,034,440 bytes; Wang LOSO file returned HTTP 404; SSH reset before key exchange. |
| 2026-08-13 | Codex with bundled LaTeX and PDF workflows | Created a separate current-status YHSA-format A4 report without modifying the IEEE manuscript; added descriptive site-composition and example-connectome figures from the frozen ABIDE-I archive; reused the seven audited scientific figures and eight evidence tables; compiled the 25-page PDF and inspected every rendered page. | The researcher must revise the prose in their own voice, complete supervisor and Claude/Cursor disclosure details, verify personal information, and sign the integrity page before any external submission. No new model fit, prediction, or inferential analysis was performed. | `paper/yhsa`; `output/pdf/Comp-183-Research Report-Current Status.pdf`; IEEE manuscript SHA-256 remained `435c903e...64fc4`. |

| 2026-08-13 | Codex | Froze the E0 extension registry and E1 accepted-checkpoint intervention contract; implemented deterministic map, topology, and encoded-node controls; replaced an inefficient random-access archive extractor with a verified single-pass extractor; added score-blind per-site and full-run auditing; restored temporary SSH access; and deployed the tested extension to AWS. The 20-checkpoint CALTECH reproduction preflight and complete 100-permutation/four-density smoke both passed without displaying scientific values. After the smoke projected the full workload within the timing gate, Codex launched the resumable 18-site E1 manager with required SNS start/terminal alerts. | Preserve the Study 1 archive unchanged. Do not read E1 predictive or representation values until all 18 site artifacts pass their independent score-blind audits and the manager completion certificate exists. Remove temporary SSH rules when remote work ends. | `configs/extensions`; `src/extensions/e1_interventions.py`; `scripts/prepare_e1_intervention_inputs.py`; `scripts/run_e1_interventions.py`; `scripts/audit_e1_interventions.py`; `scripts/run_e1_full_manager.py`; AWS runs `e1_reproduction_preflight_v1`, `e1_caltech_smoke_v1`, and active manager `e1_full_v1`. |
| 2026-08-14 | Codex with bundled LaTeX and PDF workflows | Retrieved and hash-verified the completed 18-site E1 AWS run; froze the analysis protocol before opening scientific values; implemented and unit-tested equal-site aggregation, paired bootstrap intervals, exact sign-flip tests, and Holm correction; unblinded once with the required acknowledgement; independently recomputed and audited all tables and figures; then created a separate E1-complete YHSA-format report without modifying the existing IEEE manuscript or current-status YHSA report. | Five analysis tests passed before unblinding; every source artifact and score-blind audit hash was checked; the independent analysis audit passed; the 29-page report compiled, extracted without unresolved references, rendered page-by-page, and was visually inspected. The AWS console session had expired, so the idle instance could not be stopped from Codex and still requires a manual stop. | `configs/extensions/e1_analysis_v1.json`; `configs/extensions/e1_analysis_execution_v1.json`; `src/extensions/e1_analysis.py`; `scripts/analyze_e1_interventions.py`; `scripts/audit_e1_analysis.py`; `tests/extensions/test_e1_analysis.py`; `docs/extensions/e1_results.md`; `docs/extensions/e1_claim_ledger.md`; `paper/yhsa-e1`; `output/pdf/yhsa-e1/report.pdf`; sealed E1 run and analysis archives in `outputs/archives`. |
| 2026-08-14 | Codex with bundled LaTeX and PDF workflows | Froze, implemented, tested, deployed, monitored, and analyzed the E2 synthetic known-geometry study. Built seven matched operators, seven generator families/controls, ten paired seeds, score-blind checkpoints and audits, 100 replicate seals, frozen paired inference, and a separate E1+E2 report. During pre-result review, Codex found that the first S0 implementation used reflections in half its nominal identity maps; the v1 run was stopped before unblinding, quarantined, disclosed, regression-tested, and restarted completely as v2. | Thirteen E2 tests and 65 legacy tests passed; cached synthetic operators matched the original implementation; local and AWS smokes created no test predictions; all 700 v2 cells and 100 replicate groups passed independent score-blind audits; the analysis was unblinded once and independently recomputed; both sealed archives matched their AWS hashes; the 34-page PDF contained no unresolved references and every page was rendered and inspected. | `configs/extensions/e2_synthetic_geometry_v1.json`; `configs/extensions/e2_analysis_v1.json`; `configs/extensions/e2_execution_v2.json`; `src/extensions/e2_synthetic.py`; `src/extensions/e2_training.py`; `src/extensions/e2_analysis.py`; `scripts/run_e2_synthetic.py`; `scripts/audit_e2_full_run.py`; `scripts/analyze_e2_synthetic.py`; `scripts/audit_e2_analysis.py`; `docs/extensions/e2_pre_result_amendment_v2.md`; `docs/extensions/e2_results.md`; `docs/extensions/e2_claim_ledger.md`; `paper/yhsa-e2`; `output/pdf/yhsa-e2/report.pdf`; accepted run `e2_synthetic_full_v2`. |
| 2026-08-14 | Codex with project-local Humanizer guidance and bundled LaTeX/PDF workflows | Created a non-destructive final report from the accepted E2 report; corrected the Study 1/E1/E2 evidence boundary; audited report numbers against the frozen snapshot and sealed-extension records; verified Wang, Parisot, Yang, and Han protocol differences from primary sources; added a protocol-aware literature table; and documented the ABIDE-II compatibility gate. | All four sealed E1/E2 archive hashes matched their manifests. The intended repository suite passed 98 tests. The 36-page PDF compiled with no unresolved references, all pages were rendered and inspected, and the final source, audit documents, and extracted text passed the privacy scan. No model fitting, prediction, unblinding, or accepted-result change occurred. | `paper/yhsa-final`; `output/pdf/yhsa-final/report.pdf`; `docs/final_claim_and_literature_audit.md`; `docs/literature_protocol_comparison.md`; `docs/abide_ii_compatibility_gate.md`; `reproducibility/final_report_manifest.json`; `paper/references.bib`. |

## Required fields for future entries

- Date and tool/version where known
- Specific task performed
- Files, code, results, or decisions affected
- What was independently checked
- Link to retained chat, prompt, or run artifact when available
